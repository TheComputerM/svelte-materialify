<script>
  import { Icon } from 'svelte-materialify';
  import { mdiAccount, mdiAlert, mdiBackspace, mdiBattery } from '@mdi/js';
</script>

<div class="d-flex justify-space-between">
  <Icon path={mdiAccount} />
  <Icon path={mdiAlert} />
  <Icon path={mdiBackspace} />
  <Icon path={mdiBattery} />
</div>
